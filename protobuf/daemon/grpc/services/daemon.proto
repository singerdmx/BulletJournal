syntax = "proto3";

package services;

import "grpc/types/join_group_events.proto";
import "google/api/annotations.proto";

option go_package = "github.com/singerdmx/BulletJournal/protobuf/grpc/services";
option java_multiple_files = true;
option java_package = "com.bulletjournal.protobuf.grpc.services";
option java_outer_classname = "Daemon";

// The daemon service definition.
service Daemon {

  // Serve rpc call sending JoinGroupEvents
  rpc JoinGroupEvents (types.JoinGroupEvents) returns (types.ReplyMessage) {}

  // Serve rest call sending JoinGroupEvents
  rpc Rest (types.JoinGroupEvents) returns (types.ReplyMessage) {
     option (google.api.http) = {
          post: "/daemon"
          body: "*"
     };
  }
}